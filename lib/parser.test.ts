import { describe,expect,it } from "vitest";
import { classifyCategory,classifyPromotion,deduplicatePromotions,parsePromotions } from "./parser";
describe("Maxi Zoo promotieparser",()=>{it("classificeert Belgische foldercategorieën",()=>{expect(classifyCategory("natvoer voor katten")).toBe("Cat");expect(classifyCategory("aquarium voor vissen")).toBe("Fish");});it("herkent promotietypes",()=>{expect(classifyPromotion("2e aan -50%")).toBe("Multi-buy");expect(classifyPromotion("Friends prijs")).toBe("Friends price");});it("extraheert promoties met bronpagina",()=>{const p=parsePromotions("--- PAGE 2 ---\nWhiskas natvoer  2e aan -50%");expect(p[0].sourcePage).toBe(2);});it("verwijdert duplicaten",()=>{const p=parsePromotions("--- PAGE 1 ---\nVoer  2e aan -50%");expect(deduplicatePromotions([...p,...p])).toHaveLength(1);});});
